# Core dependencies for Streamlit dashboard
streamlit>=1.38.0
pandas>=2.0.0

# Optional: Only needed if running pipeline (not needed for dashboard only)
# openai>=1.0.0
# pydantic>=2.0.0
# python-dotenv>=1.0.0
# beautifulsoup4>=4.12.0

